# Лабораторна робота №3  
## Розгортання програмного середовища та збірка ПЗ для ARM (BeagleBone Black)  


### 🎯 Мета роботи  
Ознайомитися з процесом збірки завантажувача **U-Boot**, ядра **Linux** та мінімальної файлової системи на основі **BusyBox** для архітектури ARM, а також виконати запуск побудованої системи в емуляторі **QEMU**.

---

### 📂 Зміст репозиторію  
- `configs/` — конфігураційні файли:  
  - `u-boot.config`  
  - `linux.config`  
  - `busybox.config`  
- `init/rcS` — початковий скрипт запуску `rootfs`.  
- `screenshots/` — скріншоти роботи системи.  
- `README.md` — опис виконання роботи.  

---

### 🚀 Короткий опис виконаних етапів  
1. Встановлено ARM-кроскомпілятор, завантажено вихідні коди **U-Boot**, **ядра Linux** та **BusyBox**.  
2. Налаштовано конфігурації та виконано збірку завантажувача, ядра та `rootfs` на основі BusyBox у **статичному режимі**.  
3. На основі створеної файлової системи сформовано `initramfs` (`rootfs.cpio.gz`), після чого здійснено успішне завантаження ядра Linux в емуляторі **QEMU** з робочою командною оболонкою BusyBox.

---

### 📌 Висновок  
В результаті роботи повністю зібрано та запущено мінімальну ARM-систему на базі Linux. Всі етапи — від підготовки інструментів до тестування в емуляторі — було виконано успішно. Робота закріпила практичні навички у **кроскомпіляції**, **конфігурації ядра** та формуванні `initramfs`.
